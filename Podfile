# Uncomment the next line to define a global platform for your project
#  platform :ios, '9.0'

target 'LiveStreaming' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  #Pods for LiveStreaming， pod install 其他第三方库时，为防止之前pod的旧版本因更新版本后，部分API无法使用，xcode会自动生成Podfile.lock，该文件能够保证在下一次 pod install时不更改之前pod的旧版本
  pod 'Alamofire'  , '~> 4.0'          #网络请求 4.9.1 (主要版本4，次要版本9，修订版本1)，版本范围：4.0～5.0
  pod 'Kingfisher' , '~> 4.0'          #下载网络图像 4.10.1 ，版本范围：4.0～5.0

end
